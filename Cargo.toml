[package]
name = "fieldforge"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14.*", features = ["dynamic_linking"] }
bevy_egui = "0.29"
tessellation =  "0.8.*"
nalgebra = "0.22.1"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3